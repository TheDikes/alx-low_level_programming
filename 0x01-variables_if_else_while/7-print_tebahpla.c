#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: remark
 */
int main(void)
{
	char Reverse; for (Reverse = 'z';  Reverse >= 'a'; Reverse--)
		putchar(Reverse);
	putchar('\n');
	return (0);
}
