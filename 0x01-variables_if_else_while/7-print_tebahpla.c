#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: remark
 */
int main(void)
{
	char Reverse; for (Reverse = 'a';  Reverse <= 'z'; Reverse--)
		putchar(Reverse);
	putchar('\n');
	return (0);
}
