#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: int to be multiplied with b
 * @b: int to be multiplied with a
 * Return: multiples of the result
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
